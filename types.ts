export interface NavItem {
  label: string;
  href: string;
}

export interface Industry {
  title: string;
  description: string;
  icon: string;
}

export interface Operation {
  category: string;
  icon: string;
  items: string[];
}
